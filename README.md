# Meu primeiro teste DevOps Jr

Este repositório contém meus testes com Git e Docker.

## Comandos utilizados
- `git add README.md`
- `git commit -m "mensagem"`
- `git push origin main`
- `git rebase --continue`
